# Wave pocket → breaking zone (stub).
# TODO: wrap third_party/ai-wave-tracker inference to produce wave_id and breaking-zone mask
def breaking_zone_mask(frame):
    # return binary mask and optional wave direction vector
    return None, None
