# DeepSORT/ByteTrack glue (stub)
# TODO: keep track states, velocities, and smoothing
class Tracker:
    def update(self, detections):
        # return list of tracks: {id, bbox, vx, vy, speed}
        return []
